/** Longest thank-you message a button may show, in characters. */
export const MAX_THANKS_MESSAGE_LENGTH = 160;

/**
 * What a button says under itself once a visitor has used all their clicks,
 * unless its owner writes their own. Migration 012 gives existing buttons the
 * same text.
 */
export const DEFAULT_THANKS_MESSAGE = "Thank you so much, we're truly grateful.";
